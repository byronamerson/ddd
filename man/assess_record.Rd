% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assess_record.R
\name{assess_record}
\alias{assess_record}
\title{Assess whether a temperature record supports fitting at a target period}
\usage{
assess_record(
  x,
  period,
  max_gap_frac = 0.17,
  min_cycles = 2,
  gap_threshold = 3
)
}
\arguments{
\item{x}{A zoo object (single-column). Index should be numeric (seconds)
or POSIXct.}

\item{period}{Numeric: target period in seconds. Common values:
86400 (diel), 31557600 (annual = 365.25 * 86400). No default; user
must specify.}

\item{max_gap_frac}{Numeric: maximum tolerable gap as a fraction of
\code{period}. Default 0.17.}

\item{min_cycles}{Numeric: minimum contiguous record length expressed as
multiples of \code{period}. Default 2.0.}

\item{gap_threshold}{Numeric: multiplier on median sampling interval to
detect gaps. Default 3.}
}
\value{
A list with class \code{"record_assessment"} containing:
\describe{
\item{segments}{A tibble with columns: \code{seg_id}, \code{start}, \code{end},
\code{duration_s}, \code{n_obs}, \code{cycles}, \code{verdict}.}
\item{gaps}{A tibble with columns: \code{gap_id}, \code{start}, \code{end},
\code{duration_s}, \code{frac_of_period}, \code{tolerable}.}
\item{overall_verdict}{Character: one of "sufficient", "marginal",
"insufficient".}
\item{params}{List of the parameter values used.}
}
}
\description{
Examines a zoo time series for gaps and total length relative to a target
period (diel, annual, or arbitrary). Returns a diagnostic object describing
contiguous segments, gap locations and durations, and a per-segment verdict
on whether the record is sufficient for fitting.
}
\details{
\strong{Gap detection:} A gap is any interval between consecutive observations
that exceeds \code{gap_threshold} times the median sampling interval.

\strong{Segment classification:} Contiguous segments between gaps are classified:
\itemize{
\item "sufficient" — segment spans >= \code{min_cycles} complete periods
\item "marginal" — segment spans >= 1.0 but < \code{min_cycles} periods
\item "insufficient" — segment spans < 1.0 periods
}

\strong{Overall verdict:} "sufficient" if any segment qualifies, "marginal"
if any segment >= 1.0 cycles but none meets \code{min_cycles}, "insufficient"
otherwise.

\strong{Gap tolerability:} A gap is tolerable if its duration is less than
\code{max_gap_frac * period}.
}
\references{
Johnson, Z. C. et al. (2021). Heed the data gap: Guidelines for using
incomplete datasets in annual stream temperature analyses. Ecological
Indicators, 122, 107229.
}
